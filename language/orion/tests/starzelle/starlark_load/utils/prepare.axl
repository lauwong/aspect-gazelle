load("//tests/starzelle/starlark_load/utils:identity.axl", "identity")

def _prepare(_):
    return aspect.PrepareResult(
        sources = identity([
            aspect.SourceGlobs("**/*.*"),
        ]),
    )

prepare = _prepare
