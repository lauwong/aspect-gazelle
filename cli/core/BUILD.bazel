# Run gazelle to create bzl_libraries for all packages below this one
# NB: keep in sync with top-level gazelle:lang directive
# gazelle:lang aspect-configure,js,go,proto,starlark
exports_files(["LICENSE"])
